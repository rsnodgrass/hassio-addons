---
configuration:
  KEEPALIVE_CHECK_IP:
    name: KEEPALIVE_CHECK_IP
    description: Check IP
  KEEPALIVED_CHECK_PORT:
    name: KEEPALIVED_CHECK_PORT
    description: Check port
  KEEPALIVED_INTERFACE:
    name: KEEPALIVED_INTERFACE
    description: "For example end0"
