# DO NOT EDIT!
# default configuration for a bridge instance (override these in config.yaml)

bridge:
  port: 5000
  
  # Default to listen on all interfaces. Note this may be insecure if run outside the
  # context of a Docker container since this is accessible on all network interfaces.
  #   host: 0.0.0.0

flask:
  debug: true # WARNING: do not use debug mode in production
  
restplus:
  swagger_ui_doc_expansion: "list"
  validate: true
  mask_swagger: false
  error_404_help: false
